# Homebrew Tap for ConSol

ConSol is the smart contract console for Solidity and EVM development.

```bash
brew tap luweiCN/consol
brew install consol
consol --help
```

This tap maps to `github.com/luweiCN/homebrew-consol`.

The main project lives at `github.com/luweiCN/consol`.
